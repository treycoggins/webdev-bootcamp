// The Document Object Model
  // the DOM is a javascript representation of a webpage
  // the DOM are a collection of objects you can interact with via JS
  // every element in a webpage is an object in the DOM

  // the top level is 'window'
  // the top level object of the DOM is document
  // the document object contains tons of useful methods and properties
  // use console.dir to view the properties of the objects in the DOM
console.dir(document);








