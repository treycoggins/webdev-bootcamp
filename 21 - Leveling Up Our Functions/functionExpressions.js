// Function Expressions
  // standard functions:
  function add(x, y) {
    return x + y;
  }
  add(3,4); // returns 7

  // Function expressions store the value of functions in a variable
  // Function expressions don't have a name, they are anonymous functions
  const add2 = function (x, y) {
    return x + y;
  };

  // functions are just values that can be stored the same as numbers, strings, and arrays

